Screenshots for the Android view-props issue. Nitro's own example app,
same build, `enableExclusivePropsUpdateAndroid = true`.

- `before.png` — unfixed: TestView is solid red and upright
- `after.png`  — fixed: TestView is faded and rotated, matching the RN View
- `before-after.png` — the two side by side
